source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in active_html_tags.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

gem 'bump'

# To use a debugger
# gem 'byebug', group: [:development, :test]

gem 'rubocop', '~> 1.57'
